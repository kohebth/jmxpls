# Security

Workspace guards restrict file access to configured roots. Execution tools must use argument arrays, reject unsafe arguments, and default to disabled execution policy. Secret-like keys are redacted in summaries and logs.
