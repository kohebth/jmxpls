# Component Adapters

Built-in descriptors cover core plan structure, controllers, HTTP, data/config, samplers, timers, assertions, extractors, processors, and listeners. Unknown components fall back to raw preservation.
