# Plugin Support

Unknown plugin nodes are preserved by default. Dynamic catalogs can supplement built-in descriptors when JMeter plugin jars are available. Strict JMeter validation should report missing plugin classes clearly.
