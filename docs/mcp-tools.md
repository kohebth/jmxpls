# MCP Tools

The server exposes session, query, Plan Language, mutation, raw, catalog, validation, execution, template, and typed component tool descriptors. Tools are schema-first and should avoid returning raw XML unless a raw tool is explicitly requested.
